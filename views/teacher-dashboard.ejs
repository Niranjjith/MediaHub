<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <title>Teacher Dashboard</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      background: linear-gradient(135deg, #ffffff, #e9e4f0);
      font-family: 'Segoe UI', 'Roboto', sans-serif;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
      color: #111;
    }

    .glass-container {
      width: 100%;
      max-width: 900px;
      padding: 3rem 2rem;
      background: rgba(255, 255, 255, 0.15);
      border-radius: 20px;
      box-shadow: 0 15px 35px rgba(0, 0, 0, 0.2);
      backdrop-filter: blur(18px);
      -webkit-backdrop-filter: blur(18px);
      border: 1px solid rgba(255, 255, 255, 0.3);
      text-align: center;
      transition: all 0.3s ease;
    }

    header {
      font-size: 2.5rem;
      font-weight: bold;
      margin-bottom: 1rem;
      color: #222;
      animation: fadeIn 1s ease;
    }

    .info {
      font-size: 1.2rem;
      margin-bottom: 2rem;
      color: #333;
      animation: fadeIn 1.3s ease;
    }

    blockquote {
      font-size: 1.3rem;
      font-style: italic;
      color: #555;
      margin: 2rem auto;
      max-width: 600px;
      position: relative;
      padding: 1.5rem 2rem;
      border-left: 5px solid #6c5ce7;
      background: rgba(255, 255, 255, 0.25);
      border-radius: 12px;
      animation: fadeIn 1.6s ease;
    }

    .button-group {
      display: flex;
      justify-content: center;
      gap: 1.5rem;
      margin: 3rem 0;
      animation: fadeIn 2s ease;
      flex-wrap: wrap;
    }

    .btn {
      padding: 0.9rem 2.2rem;
      border-radius: 40px;
      color: white;
      text-decoration: none;
      font-weight: 600;
      font-size: 1rem;
      transition: all 0.3s ease;
      border: none;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
    }

    .btn.notice {
      background-color: #6c5ce7;
    }

    .btn.notice:hover {
      background-color: #0f096f;
      transform: translateY(-3px);
    }

    .btn.note {
      background-color: #00b894;
    }

    .btn.note:hover {
      background-color: #009970;
      transform: translateY(-3px);
    }

    .logout-btn {
      margin-top: 2rem;
      background-color: #d63031;
      padding: 0.8rem 2rem;
      border-radius: 40px;
      color: white;
      font-weight: bold;
      text-decoration: none;
      transition: all 0.3s ease;
      border: none;
    }

    .logout-btn:hover {
      background-color: #b22525;
      transform: scale(1.05);
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(-10px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @media (max-width: 600px) {
      .glass-container {
        padding: 2rem 1.2rem;
      }

      header {
        font-size: 2rem;
      }

      .info {
        font-size: 1rem;
      }

      .btn {
        width: 100%;
        padding: 0.9rem;
        text-align: center;
      }

      .button-group {
        flex-direction: column;
        gap: 1rem;
      }


      .logout-btn {
        width: 100%;
      }
    }
  </style>
</head>

<body>
  <div class="glass-container">
    <header>Welcome, <%= user.name %> (Teacher)</header>
    <div class="info">
      <p><strong>Email:</strong>
        <%= user.email %>
      </p>
    </div>

    <blockquote>
      "A good teacher can inspire hope, ignite the imagination, and instill a love of learning."
    </blockquote>

    <div class="button-group">
      <a href="/upload-notice" class="btn notice">📢 Upload Notice</a>
      <a href="/upload-note" class="btn note">📚 Upload Note</a>
      <a href="/guide" class="btn tech" style="background-color: #0d6efd; color: white;">📰 Tech Updates</a>
    </div>

    <a href="/" class="logout-btn">Logout</a>
  </div>
</body>
</html>