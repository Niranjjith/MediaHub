<!DOCTYPE html>
<html lang="en">
<head>
  <title>Edit Student</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container py-4">
  <h3>Edit Student Info</h3>
  <form action="/admin/edit-student/<%= student._id %>" method="POST">
    <div class="mb-3"><input name="name" class="form-control" value="<%= student.name %>" required></div>
    <div class="mb-3"><input name="email" class="form-control" value="<%= student.email %>" required></div>
    <div class="mb-3">
      <select name="semester" class="form-select">
        <% for (let i = 1; i <= 6; i++) { %>
          <option value="Semester <%= i %>" <%= student.semester === 'Semester ' + i ? 'selected' : '' %>>
            Semester <%= i %>
          </option>
        <% } %>
      </select>
    </div>
    <button class="btn btn-primary">Update</button>
    <a href="/admin" class="btn btn-secondary">Cancel</a>
  </form>
</body>
</html>
